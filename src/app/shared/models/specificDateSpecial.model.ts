export interface SpecificDateSpecial {
        active: boolean;
        display: boolean;
        specials: Array<string>;
        date:string;
        start:number;
        end:number;
        formattedTime: string;
}